import 'package:flutter_test/flutter_test.dart';


void main() {
  test('adds one to input values', () {
    // final scInput = StreamController();
    // final scToken = StreamController();
    // final scOutput = Stream<String>.empty();
    // final calculator = UnnuSpeechWidget(
    //     input: scInput.sink, nextToken: scToken.sink, output: scOutput);
    // expect(calculator, isNotNull);
  });
}
